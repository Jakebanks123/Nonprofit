{
  "name": "unlock-gov",
  "version": "0.1.0",
  "description": "Check what UK benefits and local council support you may be able to get",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i src/input.css -o dist/style.css --minify",
    "watch": "tailwindcss -i src/input.css -o dist/style.css --watch",
    "test": "node verify-maths.cjs && node verify-edgecases.cjs && node verify-ui.js && node verify-keyboard.js && node test.js",
    "test:maths": "node verify-maths.cjs",
    "test:edgecases": "node verify-edgecases.cjs",
    "test:ui": "node verify-ui.js",
    "test:keyboard": "node verify-keyboard.js",
    "test:scenarios": "node test.js",
    "test:browser": "node verify-ui.js && node verify-keyboard.js && node test.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "playwright": "^1.62.1",
    "tailwindcss": "4.3.3"
  }
}
